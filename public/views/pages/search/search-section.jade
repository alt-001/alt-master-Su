#search(ng-controller="searchCtrl")
  .close.text-right
    a(ng-href="" onclick="goBack()")
      img.img-responsive(src='/assets/images/follow_cross.png')
  .title
    | Please type here for a lovely thing...
  label
    input(type='text' ng-model='criteria' autofocus)
  .results
    header(ng-show="criteria.length >= 1")
      .col-xs-6.col-sm-6.col-md-6.col-lg-6
        h2 {{sectionText}}
      .col-xs-6.col-sm-6.col-md-6.col-lg-6.text-right
        h4(ng-hide="searchFiltered.length == 0")
          a(ng-href="/search?target={{criteria}}") Back to all results
      .clearfix
    ul 
      li.col-xs-6.col-sm-4.col-md-3.col-lg-2(ng-repeat="search in searches | filter: criteria | orderBy: sectionValue" ng-show="criteria.length >= 1") 
        .search-result
      .clearfix
script.
  function goBack() {
    window.history.back()
  }